# dfa2instruct

This repo contains the code for dfa2instruct, a framework for generating datasets of automata-instruction pairs.

Run the following command to see how to run the script.

```
cd dfa2instruct.py
python dfa2instruct.py --help
```

# Citation

Under review.
