[project]
name = "dfa2instruct"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "dill>=0.4.1",
    "dfax==0.1.14",
]

[project.optional-dependencies]
cuda = [
    "dfax[cuda]==0.1.14",
]
